#include "../inc/libmx.h"
// #ifdef DEBUG
// #endif

int mx_quicksort(char **arr, int left, int right)
{
    int piv = arr[(left + right) / 2];
    int l = left;
    int r = right;
    while (mx_strcmp(arr[l++]) )
    {
        /* code */
    }
    
}

